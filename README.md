# 顺诚AI工作平台

美客多跨境电商（顺诚）内部工作平台。

现有板块：
- **选品中心** —— 日用百货四大场景品类 + 一级品类市场分析（品类分析 / 店铺布局）
- **店铺矩阵** —— 18 家店的健康与风险对照 + 店铺规划（定位 / 品类 / 人群）
- **经营数据** —— 经营概览、数据体检、作业节奏、独立核算、产品核算
- **店铺合伙** —— 两位合伙人的损益结构与增量分红机制（金额待接入）
- **库存管理** —— 待接入

线上地址：https://lingkongwangyuan.github.io/shuncheng-platform/

---

## 目录结构

```
├── index.html          选品中心 · 品类总览
├── market.html         选品中心 · 市场分析（品类分析 + 店铺布局）
├── kitchen.html        厨房用品
├── bathroom.html       卫生间用品
├── livingroom.html     客厅用品
├── bedroom.html        卧室用品
│
├── shops.html          店铺矩阵 · 店铺总览
├── shops-plan.html     店铺矩阵 · 店铺规划（存量定位 + 拟新开）
├── shops-group.html    店铺矩阵 · 店群分析
│
├── ops.html            经营数据 · 经营概览
├── ops-quality.html    经营数据 · 数据体检
├── ops-rhythm.html     经营数据 · 作业节奏
├── acct.html           经营数据 · 独立核算（店铺经营核算）
├── acct-products.html  经营数据 · 产品核算（SKU 级，数据待接入）
│
├── parter.html         店铺合伙 · 合伙总览（含分红规则）
├── parter-liyuan.html  店铺合伙 · 李源
├── parter-shi.html     店铺合伙 · 石老师
│
├── assets/
│   ├── theme.css         设计系统（全站唯一样式源）
│   ├── layout.js         顶部栏 + 左侧导航（统一注入）
│   ├── render.js         选品中心渲染（品类总览 / 市场分析 / 场景品类页）
│   ├── render-shops.js   店铺矩阵渲染（店铺总览 / 店群分析）
│   ├── render-plan.js    店铺规划渲染
│   ├── render-ops.js     经营数据渲染
│   ├── render-acct.js    独立核算渲染
│   └── render-parter.js  店铺合伙渲染
│
├── data/
│   ├── categories.js     品类树 / 导航 / 店铺池 / 市场分析页结构（唯一内容源）
│   ├── shops.js          店铺矩阵数据（自动生成，勿手改）
│   ├── plan.js           店铺规划数据（自动生成，勿手改）
│   ├── ops.js            经营数据（自动生成，勿手改）
│   ├── acct.js           独立核算数据（自动生成，勿手改）
│   └── parter.js         店铺合伙数据（自动生成，勿手改）
│
└── _tools/
    └── check_contrast.py  配色对比度门禁（下划线开头，不会发布到线上）
```

十七个页面都是 30 行左右的薄壳，只负责声明「我是哪一页」：

```html
<body data-page="shops">
  <div id="sc-header"></div>     <!-- layout.js 注入 -->
  <div id="sc-sidebar"></div>    <!-- layout.js 注入 -->
  <main class="main"><div id="sc-content"></div></main>  <!-- 对应 render 脚本注入 -->
</body>
```

---

## 怎么改（不用碰页面）

| 想改什么 | 改哪里 |
|---|---|
| 加 / 删品类、改品类文案、改三级细分品类 | `data/categories.js` 的 `categories` |
| 改左侧导航（栏目、子项、顺序） | `data/categories.js` 的 `nav` |
| 改 AI 智能体清单 | `data/categories.js` 的 `agents` |
| 改某个页面的版式、颜色、间距 | `assets/theme.css` |
| 改选品中心的区块结构 | `assets/render.js` |
| 改市场分析页的两个分部（品类分析 / 店铺布局）文案与框架 | `data/categories.js` 的 `marketPage` |
| 改店铺矩阵的区块结构 | `assets/render-shops.js` |
| 改店铺规划的区块结构 | `assets/render-plan.js` |
| 改店铺规划的定位分类（铺货型 / 精品型 / 混合型） | `scripts/15_build_plandata.py` 的 `POSITIONS` |
| 改店铺规划的新店字段 | `scripts/15_build_plandata.py` 的 `"newFields"` |
| 改经营数据的区块结构 | `assets/render-ops.js` |
| **更新店铺数据** | 重新跑数据脚本（见下节），**不要手改 `data/shops.js`** |
| **更新经营数据** | 同上，跑 `scripts/11_build_opsdata.py`，**不要手改 `data/ops.js`** |

**加一个新品类的完整步骤：**

1. 在 `data/categories.js` 的 `categories` 数组里加一条（参考现有条目字段）
2. 复制任一品类页（如 `kitchen.html`），改 `data-page` 为新的 `id`
3. 在 `nav` 的选品中心 `children` 里加一行，`page` 指向新文件
4. 完成——导航高亮、面包屑、统计数字全自动

---

## 店铺矩阵（已接入真实数据）

数据来源：`数据底座/顺诚经营数据底座.xlsx` —— 3,827 条店铺日报，2026-07-01 ~ 09-19，18 家店 × 5 站点。

**更新数据只跑一条命令**（在项目根目录 `顺诚-美客多/` 下执行）：

```bash
python scripts/10_build_shopmatrix.py
```

脚本重新生成 `data/shops.js`，页面即刻生效。**不要手改 `data/shops.js`。**

### 三个计算口径（改动前请确认）

| 指标 | 算法 | 为什么这么算 |
|---|---|---|
| 色级风险率 | 红橙黄记录数 ÷ **有色级记录数** | 分母排除「未标注」，两名管理人才可比 |
| 未标注率 | 未标注记录数 ÷ 总记录数 | 单独成列，**不参与健康评分**；未标注 ≠ 健康 |
| 封停/30天 | 封停次数 ÷ 覆盖天数 × 30 | 各店记录天数差 5 倍（13 ~ 60 天），必须归一化 |

### 两个页面

| 页面 | 内容 |
|---|---|
| `shops.html` | 18 家店对照表，按「封停/30天」降序（最该管的排最前），支持按管理人筛选 |
| `shops-group.html` | 管理人对比（色级 vs 封停双轴）、站点风险分布、月度趋势、风险类型聚集、最近风险事件 |

### 已知数据问题（脚本只标注、不改数）

- **李彦辉出单口径存疑**：转化 7.23%，为所有店中位数的 6.9 倍，页面标「出单口径待核」
- **两名管理人截止日期不同**：石老师 09-18、李源 09-19，页面按各店实际日期展示
- **色级标注习惯不同**：李源 100% 标注，石老师 32% 未标注 —— 看风险率时务必同时看未标注率

---

## 经营数据（已接入真实数据）

同一份数据底座（3,827 条日报），换一个视角：**不看风险，看产出与记录习惯。**

**更新数据只跑一条命令**（项目根目录下）：

```bash
python scripts/11_build_opsdata.py
```

重新生成 `data/ops.js`。**不要手改。**

### 三个页面的分工

| 页面 | 回答什么问题 |
|---|---|
| `ops.html` 经营概览 | 在架规模多大、流量怎么分布、出单转化如何 |
| `ops-quality.html` 数据体检 | **哪些数能信、哪些不能信**（这一页最重要） |
| `ops-rhythm.html` 作业节奏 | 谁在录入、录得齐不齐（不是看谁卖得好） |

### 本模块的四条口径（改动前请确认）

1. **在架规模 = 快照，不是累计**
   取每个（店铺 × 站点）最新一条有效的「总链接数」再求和。各店快照日期可能不同（李源 09-19、石老师 09-18）。
2. **「今日上架」按作业档位呈现，不当上架量**
   全表仅 19 种取值（100 / 50 / 200 / 150 …），是"按哪个档位作业"，不是真实上架条数。
3. **「总链接数」「总出单」不跨批次**
   数据分 6 个录入 sheet，每批次内部重新累计。跨批次相连会出现假性下跌，所以只做快照、不画趋势。
4. **缺失与异常只标注、不填补**
   李彦辉出单口径异常由脚本自动检出并标「出单口径待核」，原始数值不改。

### 三个已知数据缺口（页面已列出，等补）

| 缺什么 | 影响 |
|---|---|
| 真实上架条数 | 「日均上架」对不上实际 |
| 当日出单补全（9 月缺 62%） | 转化率只能覆盖已填记录 |
| 成本 / 售价 / 物流 / 广告费 | 利润分析、广告分析无法建立 |

补齐前两项，经营分析精度才能上一个台阶；补齐第三项，才能解锁利润与独立核算。

---

## 配色铁律（改样式必须遵守）

1. **同色系禁止**：背景与文字不得同色相（浅橙底绝不配橙字）
2. **浅亮底**（相对亮度 > 0.85）文字对比度须 ≥ 4.5:1
3. **彩色浅底配深色字，深色实底才配白字**

改动样式后必须跑门禁：

```bash
python _tools/check_contrast.py
# 退出码 0 才可交付
```

该脚本会先把 `assets/theme.css` 内联，再调用对比度自检。会**自动扫描根目录下所有页面**（`_` 开头的临时文件除外），新增页面无需改脚本。

---

## 部署

`git push` 到 `main` 分支即自动上线（GitHub Pages），无需手动操作。构建约 1 分钟。

---

## 数据说明

**平台不承载编造数据。** 未接入的数据一律显示「待采集 / 待接入」占位，不做推算填充。

| 模块 | 状态 | 待接入内容 |
|---|---|---|
| 品类结构 | ✅ 已就绪 | — |
| 三级细分品类 | ✅ 已就绪 | — |
| **店铺总览** | ✅ **已接入** | 数据底座 3,827 条日报 |
| **店铺规划** | 🟡 **已上线 · 规划待填** | 决策底数为真实数据；18 家店定位、规划品类、目标人群、阶段待管理人填 |
| **店群分析** | ✅ **已接入** | 同上 |
| **经营概览** | ✅ **已接入** | 同上（快照口径，见「经营数据」章） |
| **数据体检** | ✅ **已接入** | 同上 |
| **作业节奏** | ✅ **已接入** | 同上 |
| **独立核算（店铺）** | ✅ **已接入** | 同上（效率口径，非利润） |
| 市场分析 · 品类分析 | ⏳ 待采集 | 8 个分析维度已定框架与数据源，指标未采集 |
| 店铺布局（品类→店铺） | ⏳ 待确认 | `storePool` 已列 18 家实名店铺；`market.html` 已建承接矩阵骨架，分配关系待管理人回填 |
| 产品核算 / 产品清单 | ⏳ 待接入 | 需美客多后台订单明细（Ventas），数据源已定 |
| 店铺合伙（分红） | ⏳ 待接入 | 缺 8 项金额数据；规则与四道保险丝已定 |
| 独立核算（财务口径）/ 利润分析 | ⏳ 待接入 | 缺成本 / 售价 / 佣金 / 运费 / 广告 / 退款 |
| 库存看板 / 补货预警 | ⏳ 待接入 | 缺库存数据源 |
| AI 智能体 | ⏳ 待开发 | 五个智能体均未接入 |
